{
  "watch": ["src"],
  "ext": "js,json",
  "exec": "npm run lint && node src/app.js",
  "delay": 1000,
  "restartable": "rs",
  "ignore": ["logs/*", "*.log", "*.pid", "node_modules/*"],
  "verbose": true,
  "signal": "SIGTERM",
  "env": {
    "NODE_ENV": "development"
  }
}
